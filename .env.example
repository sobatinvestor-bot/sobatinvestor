# Copy this file to .env (for local dev only, NEVER commit .env)
# For production, set this in Cloudflare Pages → Settings → Environment Variables

ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxx
